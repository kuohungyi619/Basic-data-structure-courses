// Add items using slice;

const numbers = [10, 11, 12, 13];
const startIndex = 3;
const amountToDelete = 1;

numbers.splice(starIndex, amountToDelete, 13, 14);

// The first two parameter will delete the element, and the third parameter will add the number to the list.
// From where it delete the parameter.