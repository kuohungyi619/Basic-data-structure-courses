// Splice allow us to remove any number of consecutive elements from anywhere in an array
// It can take up to 3 parameters.
// First 2 represents indexes, or positions, of the array

let array = ['today', 'was', 'not', 'so', 'great'];
array.splice(2,2)

//This will remove 'not' 'so'