// To be able to retrieve that data on command. 
// let's begin to think about how we can access that array's information.

let ourArray = ["a", "b", "c"];
let ourVariable = ourArray[0]; // This will return a
ourArray[1] = "not b anymore"; // This will replace b in the array with not b anymore


